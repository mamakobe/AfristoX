
@model IEnumerable<afriStox.Models.ShareTrades>  

    <div class="marquee">
      
 @foreach (var item in Model) {





                <span class="breadcrumb-item"><a href="#">@Html.DisplayFor(modelItem => item.company_name)</a></span>
                <span class="breadcrumb-item active" aria-current="page">@Html.DisplayFor(modelItem => item.open)</span>
           
   
 }
 </div>